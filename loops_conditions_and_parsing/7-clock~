#!/usr/bin/env bash
#Write a Bash script that displays the time for 12 hours and 59 minutes

hour=0
min=1
while [ $hour -le 12 ];
do
echo "Hour: $hour"
while [ $min -le 59 ];
do
echo $min
min=$(( min + 1 ))
hour=$(( hour + 1 ))
done
